const div = { display: 'flex' }
const div1 = { float: 'right' }
const heading = { color: 'black', marginTop: '-12%', marginLeft: '-5%', fontWeight: 'bold' }
const headdate = { marginTop: '-4%', marginLeft: '-27%', opacity: 0.4 }
const date = { color: 'black', fontWeight: 'normal' }
const profile = { marginTop: '8%', marginLeft: '-12%' }
const profhead = { fontSize: '5', color: 'black', fontFamily: 'sans-serif', fontWeight: 'bold' }
//==============================================================================================
const topButton = { color: 'white', background: '#800000', position: 'absolute', top: '5%', marginLeft: '-18%', width: '9%', fontSize: 10 }
const avatar={
      top: '3%',
      marginLeft: '-260%',
      width: '45px',
      height: '45px',
      color: 'black'
}
const sideHead = { flex: 1, flexDirection: 'column' }
    const headTop={
      marginTop: '20%'
}
const headname = { marginLeft: '-150%', fontWeight: 'bold' }
const devhead = { marginTop: '-18%' }
const dev = { marginLeft: '-150%', fontSize: 10 }
const avatarhead = { display: 'flex', justifyContent: 'center', alignItems: 'center', height: '220px' }
    const avatarinside={

      width: '100px',
      height: '100px',
      borderRadius: '10px'
}
    const ava={
      marginLeft: "-22px",
      borderRadius: '10px',
      background: 'white',
      width: '10%',
      height: '16%',
      marginTop: '10%'
}
const displayname = { display: 'flex', justifyContent: 'center', alignItems: 'center', marginTop: '-52px' }
const displayemail = { display: 'flex', justifyContent: 'center', alignItems: 'center', marginTop: '-38px' }
const listItem = { marginBottom: '-150%' }
const image = { width: '220%', marginTop: '10%' }
const icon = { float: 'right', marginLeft: '50%' }
const container={
      marginTop: '-35%', width: '150%', height: '100%'
}
    const location={
      width: '150%', height: '100%'
}
const saveButton = { color: 'white', background: '#800000', position: 'relative', bottom: '0%', }
const phone1={
      width: '150%', height: '100%'
}
const phone2 = { marginLeft: '-30%' }
const dname = { marginLeft: '-40%' }
    const dcontainer={
      width: '150%', height: '100%'
}
    const textField={ width: '80%' }
export const styles={
      div,
      div1,
      heading,
      headdate,
      date,
      profile,
      profhead,
      topButton,
      avatar,
      sideHead,
      headTop,
      headname,
      devhead,
      dev,
      avatarhead,
      avatarinside,
      ava,
      displayname,
      displayemail,
      listItem,
      image,
      icon,
      container,
      location,
      saveButton,
      phone1,
      phone2,
      dname,
      dcontainer,
      textField
}